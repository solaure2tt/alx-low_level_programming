#include "main.h"

/**
 * add - add two number
 * Description: function that adds two integers and returns the result
 * @a: first number in entry
 * @b: second number in entry
 * Return: number
 */
int add(int a, int b)
{
	int result = 0;

	result = a + b;
	return (result);
}
